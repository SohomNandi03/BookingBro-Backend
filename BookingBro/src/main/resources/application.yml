spring:
  application:
    name: BookingBro
  datasource:
    url: jdbc:mysql://localhost:3306/BookingBro_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: AmiS0h0m
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      file-size-threshold: 2KB
auth:
  token:
    jwtSecret: "362436"
    expirationInMils: 86400000
server:
  port: 9192
